apiVersion: v1
kind: Service

metadata:
  name: h2projectcrud-service

spec:
  type: NodePort

  selector:
    app: h2projectcrud

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
