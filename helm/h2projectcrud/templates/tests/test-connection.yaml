apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "h2projectcrud.fullname" . }}-test-connection"
  labels:
    {{- include "h2projectcrud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "h2projectcrud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
